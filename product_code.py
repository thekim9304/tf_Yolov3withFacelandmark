import cv2
import tensorflow as tf

# from utils.preprocess import Preprocessor
#
# # landmark value = 196 (xn, yn) * 2
#
# preprocessor = Preprocessor('./annotation_preparation/WFLW',
#                             output_shape=(416, 416),
#                             batch_size=1)
#
# imgs, labels = next(preprocessor())


print('='*50)